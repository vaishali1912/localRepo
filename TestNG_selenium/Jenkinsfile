pipeline {
    agent any

    tools {
        maven 'Maven3'
        jdk 'JDK'
    }

    stages {

        stage('Checkout') {
            steps {
                git branch: 'master',
                    url: 'https://github.com/vaishali1912/localRepo.git'
            }
        }

        stage('Build') {
            steps {
                dir('TestNG_selenium') {
                    bat 'mvn clean'
                }
            }
        }

        stage('Compile') {
            steps {
                dir('TestNG_selenium') {
                    bat 'mvn compile'
                }
            }
        }

        stage('Test') {
            steps {
                dir('TestNG_selenium') {
                    bat 'mvn test'
                }
            }
        }

        stage('Package') {
            steps {
                dir('TestNG_selenium') {
                    bat 'mvn package'
                }
            }
        }
    }

    post {
        success {
            echo 'Build Successful'
        }

        failure {
            echo 'Build Failed'
        }
    }
}